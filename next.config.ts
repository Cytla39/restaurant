import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:['www.recetasnestle.com.ec', 'cdn0.recetasgratis.net', 'e6gi275r6pm.exactdn.com']
  }
};

export default nextConfig;
